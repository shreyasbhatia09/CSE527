#include <iostream>
#include <cv.h>
#include <opencv.hpp>
#include <highgui.h>

using namespace std;
using namespace cv;

int main()
{
	// Create an image container
    Mat image;
	
	// Copy an image into the VM with something like WinSCP and change the path accordingly
    image = imread("/home/name/CSE527/test/background.jpg", IMREAD_COLOR);
	
	// Display the image
    imshow("Test", image);
    waitKey();   
   
    return 0;
}